#!/usr/bin/env bash
# Thin wrapper: run scripts/covers.py using the project venv.
set -euo pipefail
DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec "$DIR/.venv/bin/python" "$DIR/scripts/covers.py" "$@"
